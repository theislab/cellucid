/**
 * @fileoverview State serializer entry point.
 *
 * The implementation is split into focused submodules under `./state-serializer/`
 * to keep each file small and maintainable.
 */

export { createStateSerializer } from './state-serializer/core.js';

